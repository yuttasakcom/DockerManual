version: '3'

services:

  php:
    build: .
    container_name: php
    networks:
      - php
      - redis_redis
    volumes:
      - ~/workspace/www:/var/www/html
    restart: always

networks:
  php:
    driver: bridge
  redis_redis:
    external: true

version: '3'

services:

  php:
    build: .
    image: yuttasakcom/dm-php:1.0.0
    container_name: php
    networks:
      - web
      - db
    volumes:
      - ~/workspace/www:/var/www/html
    restart: always

networks:
  web:
    external: true
  db:
    external: true
